# Selectors (read side)

The read side of the app: keyword-only queries that raise domain errors. Includes
`image_with_fewest_ratings`, the [review-ordering](../concepts/review-ordering.md) lookup.

::: django_dirt_ratings.selectors
